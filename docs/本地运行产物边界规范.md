# Aoo 本地运行产物边界规范

- `work/runtime/` 只保存可再生成的构建、测试、故障注入和压测证据。
- `work/local-runtime/` 只保存本机启动时生成的配置副本、PID 与临时状态。
- 两个目录均不得作为 Maven 资源、应用类路径、容器上下文或发布包输入。
- 本地配置、SQL、JSON、脚本及可能含凭证的文件权限必须为仅所有者可读写；凭证不得复制到正式源码。
- 本地绝对路径仅允许出现在被隔离的诊断产物中，不得进入源码、配置模板或发布描述。
- 工具必须把目录视为可删除缓存，不得以其中任一文件作为生产权威源。
