# 安全删除证明规范

任何源码、配置、数据库对象、前端资源、依赖或测试的删除，必须先登记到 `config/deletion-proofs.json`，并同时具备：

1. 静态调用和反向引用为零。
2. 固定业务循环及失败路径的动态运行无命中。
3. 数据库、配置、热更新、报表和外部消费者无引用。
4. 构建产物、镜像、资源清单和发布包不再携带。
5. 受影响回归、兼容和回放测试通过。
6. 负责人、复核时间及待删制品 SHA-256 完整。

候选清单不能作为删除授权；缺少任一证据时必须保留。
