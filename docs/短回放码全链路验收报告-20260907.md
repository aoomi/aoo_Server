# 短回放码全链路验收报告（2026-09-07）

## 结论

短回放码已完成从权威小局结算、稳定分配、结果页展示与复制、跨账号输入解析，到指定小局权威回放加载的桌面和移动网页闭环。新短码为 6～8 位纯数字；旧 11 位回放码继续通过显式 `LEGACY_11` 兼容分支处理。

本次真实局使用房间 `953928`、零基 `setId=0`、终局 `stateVersion=24`、操作号 `1788768166548-80580385263028`，服务端生成短码 `207124`。账号 17（数据库玩家 397）不是该房间参与者，输入短码后取得目标级授权，并成功加载 21 帧确定座位视角回放。

## 实现边界

- `replay_short_code` 以码为主键、以 `(room_id,set_id)` 唯一键保证稳定和幂等；号码不截断旧码、不覆盖、不复用。
- 结算域一基 `roundNo=N` 显式换算为回放域零基 `setId=N-1`。
- 只有成功解析短码的登录账号才写入 `replay_short_code_access`。
- 原参与者保持自己的回放视角；非参与者按 `seat_id,player_id` 升序固定到一个原始座位，只得到公共事件和该单一座位的回放快照，客户端不能指定或切换视角。
- 历史列表、实时牌局和其他房间权限不因短码授权扩大。

## 自动验证

- 服务端受影响模块编译通过。
- `ReplayCodeJdbcIntegrationTest` 使用真实 MySQL 通过，覆盖并发唯一分配、同目标幂等、解析授权和非参与者固定最低座位视角。
- 客户端 Cocos Creator 3.8.8 TypeScript `--noEmit` 编译通过。
- `short-replay-code-flow.test.mjs`：5/5 通过。
- 本地正式服务栈迁移及重启通过：Gateway 8080/18080、Version 8095、Account 8096、Hall 8093、Social 8097。
- Creator 3.8.8 已清除代码缓存并重新编译；最终只读检查时控制台 Error/Warning 均为 0。编辑器中另有外部未保存的 `Records.prefab*`，验收过程没有保存或改写它。

## 真实 E2E 证据

- 完整真实牌局、桌面/手机结算、短码展示、真实分享按钮点击和剪贴板复制：`Client/docs/前端框架规范/历史记录与证据/证据/pdk-small-settlement-background-2026-09-07T08-02-10-792Z/report.json`
- 桌面网页，账号 17 输入 `207124` 并打开房间 `953928/setId 0` 权威回放：`Client/docs/前端框架规范/历史记录与证据/证据/short-replay-code-2026-09-07T08-17-04-629Z/report.json`
- 手机网页，局域网地址 `192.168.1.108:7456`、移动视口与真实触摸输入完成同一闭环：`Client/docs/前端框架规范/历史记录与证据/证据/short-replay-code-2026-09-07T08-17-29-964Z/report.json`

两次跨账号回放均验证：共享数字键盘、逐位真实输入、显式 `lookupType=REPLAY_CODE`、解析 HTTP 200、回放 chunks HTTP 200、首帧状态包含序号、无运行时错误。

## 测试数据清理

真实 E2E 房间均已通过测试流程解散。本任务生成的房间 `201791`、`584723`、`840509`、`869930`、`953928` 对应短码映射，以及账号 397 的短码访问授权，在证据归档后按精确房间号删除；不清理其他永久映射。

## 已知非本功能门禁

- 服务端完整仓库门禁被客户端两份既有 PDK 文件中的 4 段注释实现拦截：`CommonPdkGameLogic.ts` 两处、`CommonPdkRoomPosManager.ts` 两处。受影响模块编译和真实 MySQL 测试已单独通过。
- 客户端完整资源门禁被既有空目录 `Modules/Mail`、`Modules/Spectator` 拦截。
- 较大的 `pdk-common-room-architecture` 测试有 5 个既有失败，涉及房间职责断言、权威手牌/交互旧断言及缺失 `Games/Poker/PDK/Common/BG.meta`；其中短回放码相关断言通过。

以上残项未由本次短回放码修改引入，也未在本次授权范围内改写。
