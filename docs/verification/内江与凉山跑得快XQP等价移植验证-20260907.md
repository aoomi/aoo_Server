# 内江与凉山跑得快 XQP 等价移植验证（2026-09-07）

## 结论

内江、凉山跑得快已经按独立稳定业务编码 `NJ201`、`LS201` 完成发布，并复用唯一公共 PDK 运行时。Creator 3.8.8 资源数据库已同步；桌面端和手机触屏端均通过真实 Canvas 建房、入房、稳定编码 WSS 解散与返回大厅验收。最终本地测试房间及历史数据已清零。

## 权威身份与实现边界

| 玩法 | gameId | gameCode | XQP area/type | Provider | 公共实现 |
| --- | ---: | --- | --- | --- | --- |
| 成都跑得快 | 8 | `CD201` | - | `com.aoo.bcg.poker.PdkGameProvider:CD201` | `poker:pao-de-kuai` |
| 内江跑得快 | 629 | `NJ201` | 6/5 | `com.aoo.bcg.poker.PdkGameProvider:NJ201` | `poker:pao-de-kuai` |
| 凉山跑得快 | 90005 | `LS201` | 9/5 | `com.aoo.bcg.poker.PdkGameProvider:LS201` | `poker:pao-de-kuai` |

客户端地区资源位于 `Client/assets/Games/Poker/PDK/NJ201` 与 `Client/assets/Games/Poker/PDK/LS201`，原 `.meta` UUID 随目录迁移保留。地区层只负责规则/profile、牌堆装配和协议 envelope；判型、状态机、托管、重连、回放、结算继续由公共 PDK 实现承担。

## 发布与数据清理

- `V20260907_03`：稳定业务编码、不可变 release/index 与经授权的一次性全房间历史清理；审计备份为 `Server/work/local-runtime/db-backups/aoo_login_local-before-pdk-code-reset-20260907.sql.gz`。
- `V20260907_04`：地区玩法建房 validator 修复。
- `V20260907_05`：退休内江重复的省级计费策略；本地 seed 同步保持省级策略为 `RETIRED`，避免服务重启后再次激活。
- `V20260907_06`：地区“其他限制”允许空选。
- `V20260907_07`、`V20260907_08`：凉山建房默认值、`payerMode` 与字符串默认值规范化；最终 active index generation 为 `2026090708`。
- 首次清理删除目标房间相关数据 70,110 行，同时保留 490 个账号、19 条账本、26 个俱乐部和 40 个俱乐部成员。最终 E2E 后再次按同一授权范围清理，`aoo_hall_room`、`aoo_room_event`、`aoo_settlement`、`replay_short_code` 均为 0。

## Creator 同步

通过 Creator 3.8.8 的“开发者 → 缓存 → 清除代码缓存”触发正常全量脚本重新编译，未直接写入 Library。重新生成的 preview chunk 已包含稳定 PDK push 识别与建房规则兼容逻辑；目标 Prefab 保持用户编辑器现场，不写入登录场景。控制台未出现本次变更新增的脚本注册、缺失 UUID 或反序列化错误；仍可见项目既有的其他玩法缺图/重复 UUID/废弃属性告警。

## 真实 E2E

最终证据：`Client/docs/前端框架规范/历史记录与证据/证据/njls-business-code-e2e-2026-09-07T15-27-27-672Z/report.json`。

| 项目 | NJ201 桌面 | LS201 手机触屏 |
| --- | --- | --- |
| 页面 | `http://127.0.0.1:7456/` | `http://192.168.1.108:7456/` |
| roomId | `545645` | `278832` |
| 建房 HTTP | 200 | 200 |
| 稳定请求 | `poker.NJ201.dispatch` | `poker.LS201.dispatch` |
| 稳定推送 | `poker.NJ201.state_push` | `poker.LS201.state_push` |
| 解散 stateVersion | 1 | 1 |
| operationId | 空（WAITING 房间无回合操作） | 空（WAITING 房间无回合操作） |
| 关键 POST 次数 | 3 | 3 |

两端使用同一 `creator-preview-3.8.8`、协议 `2.0`、API `http://127.0.0.1:8080` 与 Hall/Game WSS `ws://127.0.0.1:8080/api/v2/gateway/ws`；Chrome `152.0.7977.82`。9 项断言全为 `true`，报告 `cleaned=true`、无 failure；手机端控制台无错误。桌面端 404 为大厅既有非关键资源请求，不影响本验收链。

截图：

- `Client/docs/前端框架规范/历史记录与证据/证据/njls-business-code-e2e-2026-09-07T15-27-27-672Z/NJ201-desktop-created.png`
- `Client/docs/前端框架规范/历史记录与证据/证据/njls-business-code-e2e-2026-09-07T15-27-27-672Z/LS201-mobile-created.png`

## 门禁结果

- Client Creator TypeScript `--noEmit`：通过。
- Server Poker/Gateway/Bootstrap 受影响 reactor `compile`：通过。
- 协议生成文件一致性：通过；全协议检查仅被既有 `GatewayEntryPolicy.ts` 动态局域网预览 URL 字符串规则拦截。
- Poker 定向测试的 testCompile 仍被仓库既有测试源缺少 `AypdkGameProvider`、`HbpdkSession` 阻断；本次稳定编码链已由真实 E2E 覆盖。
- fresh-schema verifier 仍存在仓库全局预期表数 237 与当前 246 的漂移；本次迁移未创建新表。
