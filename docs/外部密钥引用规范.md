# 外部密钥引用规范

普通配置只能保存 `secret://提供者/路径#版本` 形式的引用，禁止保存密码、Token、私钥或证书正文。`SecretResolver` 在使用点按提供者解析引用，返回可关闭、可擦除的短期 `SecretMaterial`。

生产提供者应连接组织批准的 KMS/Vault，使用工作负载身份和最小读取权限。密钥轮换先发布新版本引用，再撤销旧版本；日志、异常、快照和审计事件不得包含密钥正文。
