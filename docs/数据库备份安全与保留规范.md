# 数据库备份安全与保留规范

- 备份目录权限必须为 0700，文件为 0600；禁止保存明文 JSON、SQL、CSV 或日志转储。
- 受限数据使用 AES-256-CBC + PBKDF2（至少 310000 次）加密，密钥只保存在项目根外的 .secrets，禁止与密文同库。
- database/backups 同时被 Git 和发布清单排除；生产恢复必须由双人审批并在隔离环境解密。
- 每份备份必须登记所有者、数据分级、创建、到期、销毁方式、密文哈希和密钥标识。默认保留 30 天。
- 到期备份必须安全销毁；审计证据只保留不可逆哈希和操作记录。
