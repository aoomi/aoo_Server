# 房间循环堆转储验证

自动回归先执行 500 次“建房、完整局资源建立、解散关闭”的弱引用回收测试。真实环境使用 `scripts/capture-room-cycle-heap.sh <pid> <目录>` 在固定循环前后强制 GC 并生成两份 HPROF 与类直方图。

验收时按 Room、Session、玩法组件、监听器、任务和缓存类比较 retained count/size；循环结束后不得随循环次数线性增长。HPROF 可能含敏感数据，只能存入受控诊断目录并按数据生命周期销毁。
