# Aoo 后端最终业务人工验收清单

> 自动整改 A08-F02 全部通过后，仅在预发布或生产同规格环境执行。不得用代码假造人工结论。

## 0. 本地自动预验收记录（更新至 2026-08-23）

以下结果用于提前发现运行时缺陷，不替代预发布/生产同规格环境的人工签字：

- `tools/f03_local_protocol_smoke.py` 已通过账号 V2 注册、旧协议鉴权、恶意 Origin 拒绝、Hall 登录/创建角色/角色登录、亲友圈创建与查询，以及 NJPDK/CDXZMJ 建房和退出。
- `tools/f03_two_player_room_smoke.py` 已通过 NJPDK 与 CDXZMJ 两名真实账号创建、加入、双方准备，并以双方收到 `SetStart` 发牌消息作为成功条件；禁止仅凭准备响应判定开局成功。
- 本地运行修复了重复 WebSocket 握手处理器、异步 DAO 基本类型空返回、账号旧请求头兼容、游戏运行时 classpath、回放参与者迁移漏执行和跨节点玩家通知空值等真实缺陷。
- Maven Reactor 当前 23 个生产子模块全量测试通过；真实 MySQL 下的账务并发幂等、百万成员索引、房间接管和玩家视角隔离集成测试均已实际执行通过；RocketMQ 集成证据沿用已完成的专项故障注入记录。
- `tools/final_backend_audit.rb` 通过：533 个玩法登记完整，A08-F02 自动任务闭环。
- 2026-08-23 复跑五项强制门禁全部通过：`check_architecture_boundaries.rb`（22 模块）、`check_hidden_card_boundaries.rb`、`check_gameplay_security_boundaries.rb`、`check_game_random_boundaries.rb`、`final_backend_audit.rb`（533 玩法）。
- `tools/f03_fault_injection.sh` 已通过 MySQL、RocketMQ、Redis 的正常基线、短时中断快速失败和恢复后复验。
- `tools/f03_v2_authority_recovery_smoke.py` 已用两个真实本地账号建立 NJPDK V2 权威房间；三次命令原子写入三条事件及序号为 3 的快照。`SIGKILL` 强杀节点并使租约过期后，新节点以递增 fencing token 接管，恢复前后玩家权威视角状态完全一致。
- 游戏节点现已跳过共享数据库中非本节点注册玩法的快照，避免单玩法节点因其他玩法快照启动失败；账号与游戏节点统一支持 `WS_TICKET_SECRET` 环境变量。
- `tools/verify_fresh_migrations.sh` 已从空库按版本顺序建立 23 张表；基础闭环迁移固定为 `V20260822_01__business_closure.sql`，避免 Outbox 增量先于基础表执行。全新库上的恢复、结算、百万亲友圈索引和账务 JDBC 测试均通过。
- `tools/account-soak-24h.sh` 正在执行本地 24 小时真实服务采样。首轮采样因未启用 Native Memory Tracking 已主动终止并由完整采样替代，不计为产品失败。正式采样已于 2026-08-23 00:37:34 +0800 启动，覆盖 HTTP 探针、MySQL/Redis/RocketMQ NameServer/RocketMQ Broker/MongoDB 依赖端口、RSS、JVM Native Memory、线程数和打开文件数；同时每 5 分钟执行一次 `tools/f03_local_protocol_smoke.py`，持续覆盖登录、恶意 Origin 拒绝、角色、亲友圈、内江跑得快和成都血战麻将创建/退出房间，并每 30 分钟执行一次 `tools/f03_two_player_room_smoke.py`，覆盖双人创建、加入、准备和开局，避免仅验证空闲存活。完成后执行 `tools/finalize_f03_soak.py <metrics.csv> <protocol-smoke.log> --deep-workload <two-player-smoke.log> --target-seconds 86400 --output <final-report.json>`，统一校验资源门禁、采样时长、两级业务轮次完整性及零失败。未达到 24 小时前不得标记通过，结果也不能替代生产同规格硬件长稳签字。
- 长稳开始阶段已记录 MongoDB `Account01_aoo.tagaccountinfos` 账号集合及 MySQL 亲友圈、房间事件、房间快照的精确计数，并确认所有关键 SQL 表 `CHECK TABLE` 为 `OK`；结束时必须再次采集并对比，确认业务负载真实持久化且没有表损坏。

## 1. 玩家完整链路

- 登录、游戏大厅、亲友圈、创建/加入/退出/解散房间均使用正式账号验证。
- CDXZMJ、NJPDK、SCJYMJ、XCPDK、ZJH、ZYPK 各完成至少一整局并核对小结算、大结算、战绩和回放。
- 断网、杀进程、换节点重连后，座位、手牌视角、轮次、分数和操作窗口一致。

## 2. 安全与异常

- 使用两个普通账号抓取 HTTP/WSS 数据，确认任何时刻均看不到他人暗牌、牌墙和随机种子。
- 验证伪造玩家、座位、房间、玩法版本、重复 requestId、重复/跳跃 seq 和过期 timestamp 全部被拒绝且不改变牌局。
- 验证同账号重复连接、Token/票据过期、非官方 Origin、超频请求和异常大报文按策略处置。

## 3. 数据与恢复

- 对账房卡/圈卡/金币流水与小结算、大结算，重复请求不得重复扣费或入账。
- 执行 MySQL、Redis、RocketMQ 短时中断和游戏节点强杀，核对恢复后的 fencing token、事件序号、房间权威状态及账务一致性。
- 在生产同规格硬件完成千万账号、百万成员亲友圈、24 小时长稳及跨可用区灾备验收，记录 P95/P99、RPO/RTO 和告警证据。

## 4. 签字结果

记录环境、版本清单、执行人、日期、失败项、修复版本和复验结果。全部项目通过后，才可将主任务 F03 从“待人工核验”改为“已完成”。

本地自动预验收通过不得填写正式执行人、不得代签，也不得把 F03 改为“已完成”。
# F03 正式稳定性样本重启说明（2026-08-23）

- 旧稳定性样本不计入正式验收：持续业务流量发现账号 ID 仍从 Redis 低位发号，可能与历史 MongoDB 账号冲突；同时旧进程未启用 NMT，不能形成原生内存趋势证据。
- P0 根因已修复：账号服务启动时统一读取 Redis、当前 MongoDB 与历史 MongoDB 的最大账号序列，并以最大值初始化发号下限；运行入口已强制执行账号数据初始化，MongoDB 数据库配置已统一。
- 动态验证已通过：历史账号序列为 `10477`，修复后新账号从 `10478` 连续生成；重启后继续从持久化最大值发号；账号、角色、亲友圈和房间数据均产生真实持久化增长。
- 正式样本自北京时间 `2026-08-23 01:10:12` 重新计时，账号服务 PID 为 `21010`，已启用 Native Memory Tracking。
- 正式样本包含：每 60 秒资源/NMT/依赖健康采样、每 5 分钟完整协议回归、每 30 分钟双玩家房间回归；必须连续满 24 小时且所有已启动轮次通过后方可签署本项。
- 证据目录：`work/runtime/account-soak-24h-final/`。
- 满 24 小时后的终检由 `tools/f03_auto_finalize.sh` 自动执行，输出 `final-report.json` 和 `database-final.json`；只有两个结果均为 `PASSED` 才能认定本地稳定性验收通过。
- 终检同时生成 `evidence-sha256.txt`，签署前必须重新计算并核对关键资源、业务、数据库和 NMT 证据的 SHA-256，防止验收后证据被覆盖或混用。
- 正式证据目录曾残留正式起点前的预检轮次，已在 `2026-08-23` 清除；业务日志现仅保留北京时间 `2026-08-23 01:10:12` 之后的轮次。最终轮次数必须以清理后的正式日志为准，预检结果不得计入 24 小时验收。
- 账号发号终检必须生成 `account-id-collision-final.json`：当前库与历史库账号 ID 交集必须为 `0`，当前序列不得落后于当前最大账号 ID，并且必须严格高于历史最大账号 ID 与历史序列。
- 1 小时阶段证据已通过：`61` 个连续资源样本、协议回归 `13/13`、双玩家完整房间流程 `3/3`，线程与文件句柄无增长，数据库持久化增长和账号 ID 无碰撞检查均为 `PASSED`；机器报告位于 `work/runtime/account-soak-24h-final/interim-1h-report.json`。
- 2 小时阶段证据已通过：`121` 个连续资源样本、协议回归 `25/25`、双玩家完整房间流程 `5/5`，线程与文件句柄无增长，数据库持久化增长和账号 ID 无碰撞检查均为 `PASSED`；机器报告位于 `work/runtime/account-soak-24h-final/interim-2h-report.json`。
