# 遗留入口同步封锁清单

`DeprecatedEntryPointBlocklist` 同步封锁 V1/legacy HTTP 路径、legacy/v1/old 消息命名空间、数字遗留消息号、904/9998 旧监听端口及旧协议配置开关。Gateway 在幂等占位和业务分发前执行消息封锁。

遗留源码只可用于离线对照，不得注册路由、监听器或配置读取点。新增退役入口必须同时加入此清单、网络策略和生命周期墓碑。
