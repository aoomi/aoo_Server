# 动态指标生命周期规范

动态房间和玩法指标通过 `DynamicMetricRegistry` 注册并持有可关闭 Handle。房间结束、玩法下架和组件销毁必须关闭 Handle；引用归零即注销 Meter。

禁止使用 roomId、accountId、userId、requestId、sessionId 作为标签。仅允许 gameId、playVersion、action 等有界维度，并设置总 series 上限。
