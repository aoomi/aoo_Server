# Aoo 事务传播策略

- 默认业务写入使用 `REQUIRED`：同一命令内的状态、流水与事件必须同成同败。
- 查询不得创建事务；需要一致视图时显式使用只读事务。
- `REQUIRES_NEW` 仅允许不可丢失且与主事务解耦的审计入口，当前生产代码未启用。
- 禁止 `NESTED`、`NOT_SUPPORTED`、`NEVER` 和 `SUPPORTS` 用于权威业务写入。
- 手写 JDBC 由 `JdbcTransactionBoundary` 承担同等的单边界语义，不混用代理传播。
