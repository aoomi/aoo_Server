# 历史数据导入后完整性复核规范

历史数据只能在停写窗口导入。导入过程不得关闭生产连接的外键校验，不得将约束或索引设为不可见。导入完成后必须先运行全量迁移，再启动服务；生产就绪门禁会重新验证：

- 所有权威业务表存在主键，所有索引均可见。
- CHECK 与外键约束处于 `ENFORCED` 状态。
- 外键关系不存在孤儿记录。
- 唯一键、字段非空、字段类型、时间语义和查询计划符合当前迁移基线。

任一复核失败时实例不得接收流量。禁止通过忽略错误、关闭 `FOREIGN_KEY_CHECKS` 或删除约束绕过门禁。
