# 旧任务清空与现场稽查

文档类型：当前状态与历史断点  
适用范围：Aoo 全项目  
稽查日期：2026-09-20  
当前结论：所有旧任务已撤销，所有执行 AI 已暂停；以下内容不再作为正在执行的任务。

## 1. 扯旋 CD299

状态：**部分核心 E2E 通过，整体未完成。**

已有证据：

- 服务端专项测试 `49/49` 通过，打包成功。
- 客户端运行契约测试 `9/9` 通过。
- Creator Preview/AssetDB 已同步过 CD299 横屏组件。
- 两人发牌、下注、补牌、分牌到结算的真实 Canvas E2E 通过。
- 六人混合加注、跟注、弃牌、全押、自动补牌和分牌结算通过。
- 休芒、下一局要求芒果、后加入芒果和揍芒场景已有通过证据。

尚未完成：

- 公共房间返回和离房按钮真实点击。
- 低分仅一家有分、搭不动和全部开房选项。
- 连续三次超时托管。
- 移动端和局域网真实验证。
- Creator 关闭重开后的最终同步检查。
- 主协调安排的独立完整复验。

结论：不得称为“扯旋等价移植完成”，也不得冻结。恢复时必须重新建立正式任务卡。

## 2. 跑得快与共享实时操作链路

状态：**存在大量未提交改动，完整整局 E2E 未完成。**

已有证据：

- Bootstrap 依赖链 43 模块编译通过。
- Gateway 五组定向测试 `25/25` 通过。
- 客户端未解析标识检查 617 文件通过。
- 已完成跑得快提示逻辑只读审计。

风险和缺口：

- 四客户端完整整局 Canvas E2E 未完成。
- 提示测试共 353 项，仍有 80 项失败。
- 公共排序器混入较多凉山策略，继续修改可能影响全部跑得快。
- 凉山三带一规则存在待确认冲突，但未获得新任务授权。

结论：旧任务撤销，公共提示和共享链路保持不动，等待用户重新提交问题。

## 3. 框架与凉山规则发布

状态：**接口链路验证通过，真实 UI E2E 未完成。**

已有证据：

- 定位 LS201 数值字段注册缺失导致操作时间发布失败。
- 规则监听日志出现同步成功。
- 活动数据库 UI schema 和校验器均包含 `[10000,15,20]`，默认 `10000`。
- Hall 配置接口返回与活动配置一致。
- Ruby 回归和客户端标识门禁通过。

尚未完成：

- 在真实 Cocos Canvas 中打开凉山创建房间、看到并选择 `10000秒`、创建并进入房间。
- 桌面和手机视口 E2E。

结论：不能称为最终完成，旧任务撤销。

## 4. 登录大厅

状态：**开发 AI 的真实 E2E 已通过，等待未来独立复验。**

已有证据：

- 修复创建房间文字点击误切换玩法的问题。
- 专项测试 `1/1` 通过。
- 客户端未解析标识检查 617 文件通过。
- 使用独立无头 Chrome 从游客登录进入大厅和创建房间。
- 点击“局数”文字后仍保持成都跑得快，点击凉山跑得快后正常切换并刷新规则。

当前处理：

- 相关范围进入“等待用户测试并受保护”，但尚未经过主协调独立复验。
- 未获得用户明确冻结或 Git 提交指令。

## 5. 亲友圈

状态：**专项测试通过，关键真实 E2E 不完整。**

已有证据：

- Club 模块编译成功。
- 房间模板隔离专项测试 `1/1` 通过。
- Hall 重启并健康。
- 前端单文件转译无错误。
- 账号 31 可通过真实界面进入亲友圈并展开更多菜单。

尚未完成：

- 缺少 `unionId=0` 且已有亲友圈的测试账号，保险箱隐藏未完成真实 UI 复验。
- 创建联盟、建房、解散、重建联盟的完整 Canvas E2E 未完成。

结论：旧任务撤销，现有改动视为未验收现场。

## 6. 未提交现场保护

上述 AI 均明确报告未执行 Git 提交或推送。共享工作区存在多 AI 和用户改动，禁止根据本报告执行批量回退、覆盖、清理或提交。

后续任何新任务开始前，主协调 AI 必须：

1. 明确责任 AI 和允许修改文件。
2. 检查是否触碰上述未验收现场。
3. 检查是否触碰受保护功能。
4. 为任务建立新的验收终点。
5. 禁止沿用旧任务的“完成”结论。

