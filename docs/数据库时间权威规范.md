# 数据库时间权威规范

- Java 领域时间统一使用注入的 `Clock` 与 `Instant`，写入 `DATETIME(3)`；房间快照、租约、幂等期限、账务和事件发生时间由应用权威生成。
- 纯持久化生命周期字段由 MySQL `CURRENT_TIMESTAMP(3)` 权威生成，例如连接代次更新时间、余额行更新时间和回放落库时间。
- 所有连接会话必须为 UTC，禁止依赖操作系统或数据库主机本地时区。
- 核心表禁止 `TIMESTAMP`、秒级字段和微秒以上精度混用；启动门禁从 `information_schema` 全量验证。
