# 环境配置差异白名单

开发、测试、预发、生产之间仅允许以下差异：部署环境标识、环境端点、外部 Secret 引用，以及涉及开发环境时的开发专用项。线程池容量、业务开关、协议和其他运行参数默认不得漂移。

发布流水线必须生成 `EnvironmentDifferenceReport` 并调用 `requireAccepted`。报告会隐藏 Secret 引用正文；出现未列入白名单的差异时阻止发布，不允许临时忽略。
