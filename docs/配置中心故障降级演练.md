# 配置中心故障降级演练

1. 正常发布后将完整原子配置版本集合写入本机 LKG 文件。
2. 模拟配置中心超时；只有捕获到真实远端失败后才允许读取 LKG。
3. 校验 HMAC 签名、版本集合校验和和最大存活时间；任一失败则实例停止就绪。
4. 篡改文件、替换签名、使用错误密钥和推进时钟超过有效期，确认全部拒绝。
5. 恢复配置中心，重新拉取权威发布并覆盖 LKG；记录降级开始、结束和 releaseId。

禁止使用无签名缓存、部分配置缓存或无限期缓存继续运行。
