# 运行时配置优先级规范

所有现代模块统一通过 `RuntimeConfigResolver` 解析运行时配置，固定优先级为：

1. JVM 启动参数（`-Dcanonical.key=value`）
2. 环境变量（规范键转大写并把 `.`、`-` 转为 `_`）
3. 本地配置文件
4. 配置中心已发布版本
5. 代码内安全默认值

配置键只允许小写英文、数字及分段符 `.`、`-`。必填值缺失、空值或非法键必须启动失败，不允许静默猜测。密钥不得放入默认值、配置文件或启动参数；密钥治理由独立 Secret 任务执行。
