# 业务协议强制规则

1. `*.dispatch` 仅兼容旧协议，新业务不得新增 `action + payload` 分支。
2. 新协议必须登记在 `aoo-protocol-v2.json`，并明确权限、幂等、版本和错误码。
3. 房间写操作必须校验认证玩家、房间、座位、`playVersion` 和状态版本。
4. 普通、重连、观战、回放下行必须分别经过玩家视角策略。
5. 人看配置与机器配置使用同一发布版本和内容摘要，不一致时禁止发布。
6. 账务、结算和退款使用业务唯一号，MQ 重复投递不能重复执行。
7. 客户端不得提交余额、得分、牌型、手牌、座位或完整房间状态。
