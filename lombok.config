config.stopBubbling = true

# Preserve Lombok's historical default for subclasses while making the
# equality contract explicit and suppressing callSuper ambiguity warnings.
lombok.equalsAndHashCode.callSuper = skip
