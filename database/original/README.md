# 原始数据库只读基线

本目录仅保存 2.22 数据库结构/数据转储，供离线映射、差异审计和迁移验收使用。

- 任何应用启动器、Maven 生命周期、部署脚本和数据库初始化工具均不得执行本目录 SQL。
- 可执行增量的唯一候选目录是 database/migrations；原始转储必须先导入隔离 staging schema，再通过受审迁移转换。
- 本目录被 .releaseignore 排除，不进入生产发布包。
- 文件内容禁止手工修订；映射结果写入独立生成台账。
